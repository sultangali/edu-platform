# =============================================
# Production Server Configuration
# Copy this file to /var/www/edu-platform/server/.env
# =============================================

# Server
PORT=5000
NODE_ENV=production

# MongoDB Connection
# For local MongoDB (installed on same server):
MONGODB_URI=mongodb://localhost:27017/edu-platform
# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/edu-platform

# JWT Secrets (CHANGE THESE! Generate with command below)
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_VERY_LONG_RANDOM_STRING_AT_LEAST_64_CHARACTERS
JWT_EXPIRE=7d
JWT_REFRESH_EXPIRE=30d

# Email Configuration (Gmail)
# 1. Enable 2FA on your Google account
# 2. Generate App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-char-app-password
EMAIL_FROM=EduPlatform <noreply@eduplatform.com>

# Google OAuth
# 1. Go to https://console.cloud.google.com/
# 2. Create a project
# 3. Enable Google+ API
# 4. Create OAuth 2.0 credentials
# 5. Add http://34.88.163.32 to authorized origins
# 6. Add http://34.88.163.32/api/auth/google/callback to redirect URIs
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://34.88.163.32/api/auth/google/callback

# Google Gemini AI API
# Get key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# Frontend URL
CLIENT_URL=http://34.88.163.32

# File Upload
MAX_FILE_SIZE=52428800
UPLOAD_PATH=uploads

